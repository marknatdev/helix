{
  "name": "helix-webhook",
  "description": "HELIX Receiver telemetry ingestion + admin API — standalone Express server, deployed to Render (see render.yaml), runnable locally via `node index.js`",
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "firebase-admin": "^12.0.0",
    "ws": "^8.18.0"
  },
  "private": true
}
